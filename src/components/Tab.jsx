import React, { useState } from "react";
import {
  useToast,
  Card,
  Typography,
  Grid,
  Button,
  Alert,
  Drawer,
  useDrawer,
  Space,
  Icon,
} from "@fowapps/fow-ui";

const Tab = ({ ctx, storage, extensionId }) => {

  return (
    <>
      <Alert description="This is awesome Page from extension"></Alert>
    </>
  );
};

export default Tab;
